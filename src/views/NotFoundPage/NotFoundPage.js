import s from './NotFoundPage.module.css'

const NotFoundPage = () => {
    return (
        <>
            <h1 className={s.header}>Такой страницы не найдено!</h1>
        </>
    )
}

export default NotFoundPage